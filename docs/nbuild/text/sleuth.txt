sleuth module
*************

sleuth.default_conditions()

sleuth.make_sleuth_table(kallisto_dirs, output_dir, table_name='sleuth_table.csv', condition_dict=None)

   Given the list of output dirs created by individual kallisto runs
   creates an output csv table for sleuth to use. Names of file dirs
   are used for each entry.

sleuth.run_sleuth(sleuth_table, output_dir, results_file_name='Sleuth_results.txt')

sleuth.test_for_condition(condition_dict, path)
